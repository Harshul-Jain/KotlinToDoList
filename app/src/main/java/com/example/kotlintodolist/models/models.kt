package com.example.kotlintodolist.models
/*
Naming conventions
if your kotlin file contains one class or interface it should be named in UpperCamelCase like MainActivity
but if your file contains more than one class or interface then name it similar to package name
 */
data class ToDo(val task:String,val done:Boolean)

